/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */
document.querySelector("body").style.backgroundColor = "crimson"

document.querySelector(".backpack__name").style.fontStyle = "italic"

document.querySelectorAll(".feature").forEach(item => item.style.border = '3px solid black')

document.querySelectorAll("span").forEach(span => span.style.color = 'green')


